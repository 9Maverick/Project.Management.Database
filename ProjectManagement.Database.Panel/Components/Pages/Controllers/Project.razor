@code {
    [Parameter, EditorRequired]
    public IProjectViewModel viewModel { get; set; } = null!;
}

@if(!viewModel.IsDeleted)
{
    @if (!viewModel.IsEditing)
    {

        <tr>
            <td>
                @viewModel.Project.Name
            </td>
            <td>
                @viewModel.Project.Abbreviation
            </td>
            <td>
                <button @onclick="viewModel.Edit">  Edit  </button>
                <button @onclick="viewModel.Delete">Delete</button>
            </td>
        </tr>
    }
    else
    {
        <tr>
            <td>
                <input @bind="viewModel.Project.Name" class="form-control" placeholder="Name" />
            </td>
            <td>
                <input @bind="viewModel.Project.Abbreviation" class="form-control" placeholder="Name" />
            </td>
            <td>
                <button @onclick="viewModel.Save">Save</button>
                <button @onclick="viewModel.Cancel">Cancel</button>
            </td>
        </tr>
    }
}